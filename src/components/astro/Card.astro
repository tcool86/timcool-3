---
// A static card wrapper. The original framer-motion `animate` here was
// a one-shot opacity fade-in; we render it as plain HTML to keep the
// section zero-JS.
---

<div class="app-card">
	<slot />
</div>
